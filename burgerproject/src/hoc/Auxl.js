/* hoc here means high order component
aux component is fxnl comnt which not mngd by hooks take props as input as return porps.chden no jsx in this file

*/
const aux =(props) => props.children;

export default aux;
